export interface CardItemProps {
  isRed?: boolean; // isRed prop은 boolean 타입이며, 필수는 아님
  isFind?: boolean;
}

export interface CardItemWrapperProps {
  isFind?: boolean;
}
export interface ItemBoxContentTextProps {
  isRed?: boolean;
}
